# OtterSec evaluation - Aptos MEDIUM target (combined library).
# Auto-generated 2026-05-14 from osec_aptos_class.yaml (40) + osec_aptos_medium_specific.yaml (21).
# 7 class hyps with aptos-small-only filenames (access_control.move,
# staking_pool.move, token_vault.move) auto-rewritten to sources/*.move
# so they resolve correctly against the medium engine's actual files
# (acl.move, staking.move, vault.move). DO NOT EDIT BY HAND.
# To regenerate: python3 merge_aptos_medium.py (script in scripts/).

hypotheses:
- id: APT1-borrow-global-no-auth
  class: authorization
  severity: Critical
  bug_class: borrow-global-no-auth
  engine_function: transfer_admin
  target_file: sources/*.move
  claim: 'Every `borrow_global_mut<T>(addr)` operation on a privileged resource is gated by an auth check that the caller
    is the owner of `addr` (or holds the right capability). Permissionlessly borrowing a privileged resource (admin config,
    treasury) lets anyone mutate it.

    '
  relevant_instructions: 'borrow_global, borrow_global_mut, move_from on admin/config types

    '
- id: APT2-missing-signer-check
  class: authorization
  severity: Critical
  bug_class: missing-signer-check
  engine_function: privileged_op
  target_file: sources/*.move
  claim: 'Every public entry function that performs an admin-only or owner-only mutation calls `assert!(signer::address_of(s)
    == owner)` before the mutation. Functions that take a `&signer` and never use it for an identity check are admin-bypass
    primitives.

    '
- id: APT3-signer-address-of-not-checked
  class: authorization
  severity: Critical
  bug_class: signer-not-bound-to-resource
  engine_function: deposit
  target_file: sources/*.move
  claim: 'Every operation on a per-user resource binds the signer''s address to the resource''s owner. `borrow_global_mut<UserBalance>(some_addr)`
    where `some_addr` is NOT `signer::address_of(&signer)` admits cross-account state-mutation attacks.

    '
- id: APT4-capability-stored-not-consumed
  class: authorization
  severity: High
  bug_class: cap-leak
  engine_function: transfer_admin
  target_file: sources/*.move
  claim: 'Every privileged capability with `store` ability is given out under a finite, intentional issuance plan (one cap
    per admin, destroyed on rotation). Storing a cap in a public-readable struct or in a per-user resource grants permanent
    admin to anyone.

    '
- id: APT5-acl-bypass-via-direct-entry
  class: authorization
  severity: Critical
  bug_class: acl-bypass-entry
  engine_function: transfer_admin
  target_file: sources/*.move
  claim: 'Every access-control-gated module function is reached ONLY through the gated entry. Adding a direct `public entry
    fun` that performs the same mutation without going through access_control::assert_admin is a bypass.

    '
- id: APT6-friend-module-trust
  class: authorization
  severity: High
  bug_class: friend-module-trust
  engine_function: friend_call
  target_file: sources/*.move
  claim: '`friend` declarations in Move are UNIDIRECTIONAL: `module A friend module B` grants B private-function access to
    A; it does NOT grant A access to B''s privates. The cross-module trust boundary check is: every friend module that exposes
    private helpers verifies the calling signer if the helper mutates state. A friend module called from a trusted context
    still needs its own auth checks unless it''s a pure passthrough.

    '
- id: APT7-resource-double-move
  class: state_transition
  severity: High
  bug_class: resource-double-move
  engine_function: transfer_resource
  target_file: sources/*.move
  claim: 'Every code path that moves a linear resource handles ALL early returns by either re-storing the resource or destroying
    it explicitly. Move''s borrow checker catches simple cases, but conditional branches can still leak resources or hit "you
    must consume" errors that DOS legitimate calls.

    '
- id: APT8-resource-leak
  class: state_transition
  severity: Medium
  bug_class: resource-leak
  engine_function: process
  target_file: sources/*.move
  claim: 'Every function that acquires a resource via `move_from` or `borrow_global_mut` and conditionally early-returns puts
    the resource back. Forgetting to re-store leaves the resource locked / unreachable.

    '
- id: APT9-event-emit-missing
  class: state_transition
  severity: Low
  bug_class: event-emit-missing
  engine_function: transfer_admin
  target_file: sources/*.move
  claim: 'Every state-mutating function emits an event with the relevant fields. Auditability requires events on transfers,
    role changes, parameter updates — silent state mutations are opaque to observers.

    '
- id: APT10-u64-overflow-arith
  class: arithmetic_overflow
  severity: High
  bug_class: u64-overflow-arith
  engine_function: deposit
  target_file: sources/*.move
  claim: 'Every arithmetic op on a u64 balance / counter that COULD overflow either uses a wider type (u128/u256) or has a
    pre-check. Move ABORTS on overflow (not wraps) — so unguarded overflow is a DoS, not a fund-loss. Reachable DoS is still
    a severity High.

    '
- id: APT11-u64-underflow-DoS
  class: arithmetic_overflow
  severity: Medium
  bug_class: u64-underflow-dos
  engine_function: withdraw
  target_file: sources/*.move
  claim: 'Every subtraction `a - b` on u64 values checks `a >= b` before the subtract. Underflow aborts, which DoSes legitimate
    paths (e.g. withdraw 0 from a balance of 0).

    '
- id: APT12-fixed-point-precision-drop
  class: arithmetic_overflow
  severity: Medium
  bug_class: precision-drop
  engine_function: share_value
  target_file: sources/*.move
  claim: 'Every multi-step share/reward calculation orders multiply-before- divide to preserve precision (`(a * c) / b`, not
    `(a / b) * c`).

    '
- id: APT13-cast-truncation
  class: arithmetic_overflow
  severity: Medium
  bug_class: cast-truncation
  engine_function: cast_amount
  target_file: sources/*.move
  claim: 'Every narrowing cast (u128 -> u64, u64 -> u8) checks bounds before casting. Move''s narrowing cast aborts on overflow,
    but reaching that abort path with attacker-controlled inputs DoSes the function.

    '
- id: APT14-divide-by-zero
  class: arithmetic_overflow
  severity: High
  bug_class: divide-by-zero
  engine_function: compute_ratio
  target_file: sources/*.move
  claim: 'Every division `a / b` where `b` is derived from input or state checks `b != 0` first. Division by zero aborts —
    reachable abort = DoS.

    '
- id: APT15-rounding-direction
  class: arithmetic_overflow
  severity: High
  bug_class: rounding-direction
  engine_function: convert_to_shares
  target_file: sources/*.move
  claim: 'Every share-vault function rounds in the protocol''s favor: mint/deposit rounds DOWN on shares minted (so user pays
    more assets per share), withdraw/redeem rounds UP on assets paid (so user gets fewer assets per share). Inverting direction
    drains the vault.

    '
- id: APT16-oracle-staleness
  class: invariant_property
  severity: Critical
  bug_class: oracle-staleness
  engine_function: get_price
  target_file: sources/*.move
  claim: 'Every oracle price read checks the `updated_at` timestamp is within a max-staleness window. Stale prices during
    oracle outages create arbitrage primitives.

    '
- id: APT17-oracle-zero-price
  class: invariant_property
  severity: High
  bug_class: oracle-zero-price
  engine_function: get_price
  target_file: sources/*.move
  claim: 'Every oracle read rejects zero or negative prices. Zero is often a sentinel for "feed unavailable" and silently
    rejecting it as a valid price corrupts downstream math (divide by zero, free shares).

    '
- id: APT18-oracle-decimals-mismatch
  class: invariant_property
  severity: High
  bug_class: oracle-decimals-mismatch
  engine_function: normalize_price
  target_file: sources/*.move
  claim: 'Every multi-asset price normalization uses each feed''s actual decimals, not a hardcoded 8 or 18. Mixing produces
    10^N errors.

    '
- id: APT19-missing-pause-check
  class: state_transition
  severity: Medium
  bug_class: missing-pause-check
  engine_function: deposit
  target_file: sources/*.move
  claim: 'Every user-facing state-mutating function checks the pause flag before mutating. Skipping pause on some functions
    means a paused protocol still bleeds via the unguarded paths.

    '
- id: APT20-asymmetric-pause
  class: state_transition
  severity: High
  bug_class: pause-asymmetric
  engine_function: withdraw
  target_file: sources/*.move
  claim: 'Pause applies symmetrically to deposit and withdraw. Pausing only withdrawals while deposits remain open is a rug-pull
    primitive.

    '
- id: APT21-share-inflation-first-depositor
  class: arithmetic_overflow
  severity: Critical
  bug_class: share-inflation-first-depositor
  engine_function: deposit
  target_file: sources/*.move
  claim: 'Every vault that mints shares from assets handles the first- depositor inflation: either virtual shares (constant
    offset) or a dead-shares burn on first deposit. Naive `shares = assets * total_supply / total_assets` admits the donate-1-wei-then-10^18
    inflation attack.

    '
- id: APT22-share-donation-inflation
  class: state_transition
  severity: High
  bug_class: share-donation
  engine_function: convert_to_shares
  target_file: sources/*.move
  claim: 'Every vault''s share conversion math is robust to direct token transfers (donations) into the vault. Donations that
    inflate total_assets without minting corresponding shares should not be profitable to the attacker.

    '
- id: APT23-total-supply-divergence
  class: invariant_property
  severity: High
  bug_class: total-supply-divergence
  engine_function: invariant
  target_file: sources/*.move
  claim: 'For every token-like aggregate (vault shares, governance votes, reward points), the protocol-tracked total equals
    the sum of per-user balances at every state transition.

    '
- id: APT24-missing-slippage
  class: state_transition
  severity: High
  bug_class: missing-slippage
  engine_function: swap
  target_file: sources/*.move
  claim: 'Every swap/redeem function accepts a min-out parameter and enforces it. Otherwise MEV bots arbitrage every swap.

    '
- id: APT25-no-deadline-on-permit
  class: state_transition
  severity: Medium
  bug_class: missing-deadline
  engine_function: swap
  target_file: sources/*.move
  claim: 'Every off-chain-signed action (relayed tx, permit) has a deadline timestamp and rejects past-deadline submissions.

    '
- id: APT26-withdraw-delay-bypass
  class: state_transition
  severity: High
  bug_class: withdraw-delay-bypass
  engine_function: withdraw
  target_file: sources/*.move
  claim: 'Every cooldown/unstake delay is unstoppable once requested. Functions that reset the readyAt timer on routine actions
    (transfer, claim) are unstake-delay-bypass primitives.

    '
- id: APT27-stake-double-claim
  class: state_transition
  severity: High
  bug_class: stake-double-claim
  engine_function: claim_rewards
  target_file: sources/*.move
  claim: 'Every reward-claim path marks the user''s snapshot consumed atomically with the payout. Re-entrant or two-call patterns
    where the snapshot updates AFTER the payout admit double-claim.

    '
- id: APT28-auction-bid-after-end
  class: state_transition
  severity: High
  bug_class: auction-bid-after-end
  engine_function: place_bid
  target_file: sources/*.move
  claim: 'Every bid-placing function rejects bids after the auction''s end_time. A late-bid-accepted auction allows arbitrage
    of after-end market info.

    '
- id: APT29-auction-settle-no-winner
  class: state_transition
  severity: Medium
  bug_class: auction-settle-no-winner
  engine_function: settle_auction
  target_file: sources/*.move
  claim: 'Every settle path handles the no-bid case correctly — returns the listed asset to the seller, does NOT transfer
    to the zero address or to the seller''s auction_id mapping with a 0 amount.

    '
- id: APT30-module-publisher-confusion
  class: authorization
  severity: High
  bug_class: module-publisher-confusion
  engine_function: init_module
  target_file: sources/*.move
  claim: 'Every `init_module` (or initializer pattern) verifies the caller is the expected publisher address. Modules deployable
    under multiple addresses can lead to "wrong publisher" trust bypass.

    '
- id: APT31-type-argument-confusion
  class: authorization
  severity: High
  bug_class: type-argument-confusion
  engine_function: generic_op
  target_file: sources/*.move
  claim: 'Every generic function `f<T>` that branches based on `T` (e.g. via phantom-type tags) verifies T is one of the expected
    types. Otherwise an attacker calls `f<AttackerOwnedToken>` to bypass whitelists.

    '
- id: APT32-phantom-type-trust
  class: authorization
  severity: Medium
  bug_class: phantom-type-trust
  engine_function: typed_resource_op
  target_file: sources/*.move
  claim: 'Every operation that consumes a `Resource<T>` and trusts its semantics requires a runtime check that T is an admitted/
    whitelisted type — phantom types only enforce at compile time; Move runtime can dispatch on attacker-supplied types.

    '
- id: APT33-liquidation-no-min-amount
  class: state_transition
  severity: High
  bug_class: liquidation-no-min-amount
  engine_function: liquidate
  target_file: sources/*.move
  claim: 'Every liquidation function rejects min-size violations (a liquidate of $0.01 should revert, not consume gas + emit
    events). Otherwise spammers grief the liquidation queue.

    '
- id: APT34-liquidation-bonus-overflow
  class: arithmetic_overflow
  severity: High
  bug_class: liquidation-bonus-overflow
  engine_function: compute_liq_bonus
  target_file: sources/*.move
  claim: 'Every liquidation-bonus calculation is bounded such that bonus + repay <= collateral_seized, even after rounding.
    Bonus formulas that admit `bonus > collateral` are insolvency primitives.

    '
- id: APT35-lending-interest-accrual-overflow
  class: arithmetic_overflow
  severity: High
  bug_class: lending-interest-accrual
  engine_function: accrue_interest
  target_file: sources/*.move
  claim: 'Every interest-accrual computation handles long time-since-last- accrue gracefully (caps or saturates). An untouched
    pool over months that''s then poked with `accrue_interest` could overflow u128 and DoS the pool.

    '
- id: APT36-fee-receiver-unauthorized
  class: authorization
  severity: High
  bug_class: fee-receiver-bypass
  engine_function: set_fee_receiver
  target_file: sources/*.move
  claim: 'Every fee-receiver setter is gated by admin auth AND validates the new receiver is non-zero, non-self. Letting the
    receiver be the contract itself can lock fees.

    '
- id: APT37-fee-percent-bound
  class: arithmetic_overflow
  severity: Medium
  bug_class: fee-percent-bound
  engine_function: set_fee_bps
  target_file: sources/*.move
  claim: 'Every fee-percentage setter rejects values >= 100% (10_000 bps).

    '
- id: APT38-treasury-drain
  class: authorization
  severity: Critical
  bug_class: treasury-drain
  engine_function: emergency_drain
  target_file: sources/*.move
  claim: 'Every treasury-withdrawal function checks admin auth AND limits the amount to a sane fraction or via a time-lock.
    Unbounded admin withdrawal is a treasury rug-pull.

    '
- id: APT39-governance-flashloan-vote
  class: state_transition
  severity: Critical
  bug_class: governance-flashloan-vote
  engine_function: cast_vote
  target_file: sources/*.move
  claim: 'Every governance vote-weight read uses a historical snapshot at `proposal_created_at - 1` (or analogous), not current
    state. Live-balance voting is flash-loan-manipulable.

    '
- id: APT40-proposal-replay
  class: state_transition
  severity: High
  bug_class: proposal-replay
  engine_function: execute_proposal
  target_file: sources/*.move
  claim: 'Every proposal execution transitions the proposal state from Queued to Executed atomically; second execution call
    reverts.

    '
- id: APTM1-vault-fee-recipient-confused-deputy
  class: authorization
  severity: Critical
  bug_class: fee-receiver-unauthorized
  engine_function: set_fee_recipient
  target_file: sources/vault.move
  claim: 'Every privileged setter on the Vault module uses `signer::address_of(caller)` as the auth subject. The current `set_fee_recipient(caller,
    host, claimed_admin, new_recipient)` reads `claimed_admin` as the auth subject for the admin check, where `claimed_admin`
    is an attacker-controlled parameter. Any caller can pass a known admin address as `claimed_admin`, pass the admin check,
    and overwrite `fee_recipient` to themselves.

    '
- id: APTM2-treasury-emergency-withdraw-no-auth
  class: authorization
  severity: Critical
  bug_class: treasury-drain
  engine_function: emergency_withdraw
  target_file: sources/treasury.move
  claim: 'Every treasury withdrawal function checks admin auth on the signer. `emergency_withdraw(_caller, host, amount)`
    discards `_caller` (underscore-prefixed) and performs no auth check. Any signer can call it and drain `Treasury.balance`
    down to zero.

    '
- id: APTM3-oracle-update-no-auth
  class: authorization
  severity: Critical
  bug_class: missing-signer-check
  engine_function: update_price
  target_file: sources/oracle.move
  claim: 'Every privileged setter on the Oracle module checks that the caller is an admin. `update_price(_caller, host, token,
    price)` discards `_caller` and writes `entry_ref.price = price` for any token. Any signer can set any token''s price to
    any value, enabling oracle manipulation that downstream consumers (`lending_pool`, `liquidation`) trust without validation.

    '
- id: APTM4-lending-borrow-oracle-driven-solvency-bypass
  class: authorization
  severity: Critical
  bug_class: oracle-driven-solvency-bypass
  engine_function: borrow
  target_file: sources/lending_pool.move
  claim: 'Every solvency check on `borrow` reads collateral/debt prices from an oracle that is authenticated and rate-limited.
    `borrow` calls `oracle::get_price` for both collateral and debt tokens; the oracle''s `update_price` is permissionless
    (see APTM3), so an attacker can set `debt_token` price to 0 before calling `borrow`, making `debt_val = 0` regardless
    of `debt`, passing the LTV check, and borrowing without meaningful collateralization.

    '
- id: APTM5-staking-fund-rewards-no-auth
  class: authorization
  severity: Critical
  bug_class: reward-pool-inflation
  engine_function: fund_rewards
  target_file: sources/staking.move
  claim: 'Every modification to the staking pool''s reward balance is gated by admin auth (or by accounting tied to actual
    deposits). `fund_rewards(funder, host, amount)` takes `funder` but discards it (`let _ = signer::address_of(funder)`),
    then writes `bucket.reward_pool = amount + bucket.reward_pool`. Any signer can inflate `reward_pool` to `u64::MAX` and
    then claim from it.

    '
- id: APTM6-fee-manager-distribute-no-auth
  class: authorization
  severity: Critical
  bug_class: missing-signer-check
  engine_function: distribute
  target_file: sources/fee_manager.move
  claim: 'Every accrual of recipient-claimable fees on the FeeManager module is gated by admin auth or driven by realized
    protocol revenue. `distribute(_account, host, amount)` discards `_account` and accrues `amount` proportionally to every
    configured recipient, also inflating `balance`. Any signer can call `distribute(u64::MAX)` and inflate every recipient''s
    `accrued` field to a number they can then claim against the inflated `balance`.

    '
- id: APTM7-governance-vote-replay
  class: state_transition
  severity: High
  bug_class: proposal-replay
  engine_function: vote
  target_file: sources/governance.move
  claim: 'Every governance vote is counted once per voter per proposal. `vote(voter, host, proposal_id, support)` calls `vector::push_back(&mut
    prop_ref.votes, Vote {voter, weight, ...})` without checking whether `voter` has already voted on this proposal. A voter
    can call `vote` N times for the same proposal — each call appends a Vote with `weight = staking::stake_amount(voter)`,
    so the voter''s effective weight is multiplied by N at tally time.

    '
- id: APTM8-staking-unstake-delay-bypass-via-restake
  class: state_transition
  severity: High
  bug_class: withdraw-delay-bypass
  engine_function: unstake
  target_file: sources/staking.move
  claim: 'Every staked principal sits behind `unstake_delay` from the time it was added to the pool. `stake` only sets `start_ts`
    on first stake creation; subsequent stake additions update `amount` but NOT `start_ts`. After the original delay has lapsed,
    a user can stake a fresh much-larger amount and immediately unstake the entire balance — because `current_ts >= entry.start_ts
    + delay` is evaluated against the stale (original) `start_ts`, not the time of the latest deposit.

    '
- id: APTM9-staking-reward-drift-on-amount-change
  class: state_transition
  severity: High
  bug_class: stake-reward-accounting-drift
  engine_function: stake
  target_file: sources/staking.move
  claim: 'Every stake addition or removal settles outstanding rewards against the user''s pre-change amount before mutating
    `amount`. `stake` and `unstake` modify `entry.amount` without updating `entry.last_claim_ts` or settling `entry.accumulated_rewards`,
    so the next `claim()` computes `accrued = mul_div(rate * delta, CURRENT_amount, total_staked)` — using the latest amount
    for the entire window since the original `last_claim_ts`. A user who stakes 1 wei at t=0 and stakes 1B at t=100 receives
    reward as if they held 1B for 100 seconds.

    '
- id: APTM10-lending-borrow-no-collateral-escrow
  class: invariant_property
  severity: High
  bug_class: signer-not-bound-to-resource
  engine_function: borrow
  target_file: sources/lending_pool.move
  claim: 'Every borrow on the lending pool requires the borrower to first deposit `collateral` worth of asset into an escrow
    that the protocol can seize on liquidation. `borrow(user, host, collateral_token, collateral, debt)` takes `collateral`
    as an attacker-controlled `u64` parameter and persists it directly into `Loan.collateral` with no token transfer, escrow,
    or ownership check. The borrower can claim arbitrary collateral they don''t actually hold, borrow against it, and never
    face real consequences on default.

    '
- id: APTM11-lending-interest-never-accrues
  class: invariant_property
  severity: High
  bug_class: lending-interest-accrual-overflow
  engine_function: borrow
  target_file: sources/lending_pool.move
  claim: 'Every open loan on the lending pool accrues interest over time based on its stored `rate_bps` and the delta since
    `last_accrual_ts`. Loans store `rate_bps` and `last_accrual_ts` at creation but no function reads `last_accrual_ts` or
    compounds `Loan.debt`. Debt remains constant after origination, so borrowers owe the principal indefinitely with no interest
    cost.

    '
- id: APTM12-vault-share-inflation-first-depositor
  class: arithmetic_overflow
  severity: Medium
  bug_class: share-inflation-first-depositor
  engine_function: deposit
  target_file: sources/vault.move
  claim: 'Every vault deposit mints shares that maintain a stable per-share asset price even for the first depositor (using
    a dead-share or minimum-deposit gate). `deposit` issues `minted = amount` when `total_shares == 0`, then on subsequent
    deposits issues `minted = mul_div(amount, total_shares, total_assets)`. A first depositor of 1 wei followed by a pre-arranged
    inflation of `total_assets` (any path that increases `total_assets` without minting shares) lets the attacker cause subsequent
    depositors'' `minted` to truncate to zero.

    '
- id: APTM13-events-empty-stubs
  class: state_transition
  severity: Medium
  bug_class: event-emit-missing
  engine_function: emit_proposal
  target_file: sources/events.move
  claim: 'Every state-mutating entry on the protocol emits a typed event capturing the actor, the resource affected, and the
    relevant before/after fields. Every `emit_*` function in `events.move` is an empty stub (`public fun emit_proposal(_proposer:
    address, _id: u64) {}` and similar). Every governance vote, borrow, repay, liquidation, stake, and unstake mutation across
    the protocol is silent at the event layer — observers cannot reconstruct activity from on-chain logs.

    '
- id: APTM14-liquidation-oracle-driven
  class: invariant_property
  severity: Medium
  bug_class: oracle-driven-solvency-bypass
  engine_function: liquidate
  target_file: sources/liquidation.move
  claim: 'Every liquidation requires the liquidatee''s position to be genuinely unhealthy as determined by a trusted, signed,
    rate-limited oracle. `is_liquidatable` reads `lending_pool::loan_health` which reads `oracle::get_price`; the oracle''s
    `update_price` is permissionless (APTM3). An attacker can set `debt_token` price to `u64::MAX` just before calling `liquidate`,
    force any healthy position into the liquidatable range, and seize collateral.

    '
- id: APTM15-lending-close-loan-collateral-lost
  class: invariant_property
  severity: Medium
  bug_class: resource-leak
  engine_function: close_loan
  target_file: sources/lending_pool.move
  claim: 'Every loan closure returns the borrower''s remaining collateral. `close_loan` removes the `Loan` from the loans
    vector without crediting the borrower''s collateral back. A loan with `debt=0` and `collateral>0` closes silently and
    the collateral is discarded from the protocol''s accounting.

    '
- id: APTM16-lending-remove-collateral-no-accrual
  class: state_transition
  severity: Medium
  bug_class: stake-double-claim
  engine_function: remove_collateral
  target_file: sources/lending_pool.move
  claim: 'Every collateral withdrawal first accrues outstanding interest on the loan so the post-withdrawal solvency check
    reflects true debt. `remove_collateral` reads `entry.debt` directly for the LTV check without first compounding interest
    from `last_accrual_ts`. Combined with APTM11 (interest never accrues), withdraws don''t see the real debt — but the structural
    defect is the missing accrual call regardless of whether downstream accrual is broken.

    '
- id: APTM17-rewards-fund-permissionless
  class: authorization
  severity: Medium
  bug_class: reward-pool-inflation
  engine_function: fund
  target_file: sources/rewards.move
  claim: 'Every modification to the Rewards module''s `epoch_pool` balance is gated by admin auth (or by realized protocol
    revenue). `fund(_account, host, amount)` discards `_account` and writes `state.epoch_pool = amount + state.epoch_pool`.
    Any signer can inflate `epoch_pool` to `u64::MAX`. Grants are admin-gated so the inflation alone doesn''t pay an attacker,
    but it removes the natural rate limit on legitimate claims.

    '
- id: APTM18-governance-execute-no-payload
  class: state_transition
  severity: Low
  bug_class: proposal-replay
  engine_function: execute
  target_file: sources/governance.move
  claim: 'Every executed governance proposal triggers the action that proposers and voters voted on. `execute` performs the
    quorum math, then sets `prop_ref.executed = true` and exits — no function dispatch, no parameter routing, no state mutation
    beyond the `executed` flag. Passed proposals have no effect.

    '
- id: APTM19-oracle-staleness-broken
  class: invariant_property
  severity: Low
  bug_class: oracle-staleness
  engine_function: update_price
  target_file: sources/oracle.move
  claim: 'Every oracle price write timestamps the entry so consumers can reject stale readings. `update_price` sets `entry_ref.updated_ts
    = 0` on every call (creation and update), and `get_price` does not consult `updated_ts` at all. Staleness detection is
    structurally impossible regardless of what consumers might want to do.

    '
- id: APTM20-staking-emergency-unstake-principal-lost
  class: invariant_property
  severity: Low
  bug_class: resource-leak
  engine_function: emergency_unstake
  target_file: sources/staking.move
  claim: 'Every emergency-unstake exit returns the user''s principal minus the configured penalty. `emergency_unstake` sets
    `entry.amount = 0`, sends the penalty portion to `bucket.reward_pool`, but DOES NOT credit the user with the payout (principal
    minus penalty). The user''s stake is wiped; they receive nothing.

    '
- id: APTM21-acl-mint-cap-permissionless
  class: authorization
  severity: Low
  bug_class: cap-leak
  engine_function: mint_admin_cap
  target_file: sources/acl.move
  claim: 'Every privileged capability mint is gated by an existing admin check. `acl::mint_admin_cap(host)` is `public` with
    no `signer` parameter and no auth — any caller can mint an `AdminCap { authority: host }`. The current cap consumers (`consume_admin_cap`,
    `cap_authority`) read the authority address but don''t grant powers on its own; the mint is a latent risk surface for
    any future consumer that trusts cap presence as evidence of admin.

    '
