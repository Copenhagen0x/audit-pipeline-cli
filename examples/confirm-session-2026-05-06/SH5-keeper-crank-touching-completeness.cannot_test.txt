// CANNOT_TEST: The KeeperCrank / touch_account_live_local ordering invariant described in SH5 cannot be expressed as a deterministic Rust integration test against the engine. The source at /root/audit_runs/percolator-live/target/engine/src/percolator.rs contains no public API named keeper_crank or KeeperCrank (grep confirmed zero matches). The only reachable entry-point is permissionless_progress_not_atomic (line 8754) which internally calls keeper_crank_with_request_not_atomic (line 8847, test_visible-gated). That inner function calls accrue_market_segment_to_internal (line 8941) once before the candidate loop run_keeper_phase1_candidates (line 8961), which itself calls touch_account_live_local per candidate (line 8602) before the partial-liq check. The ordering is a pure intra-function control-flow property: accrue fires once, then for each candidate touch runs then the margin check runs. There is no public observable state that distinguishes before-touch from after-touch from the test boundary, and the test_visible helpers (touch_account_live_local at line 7190, keeper_crank_with_request_not_atomic at line 8847) are only pub under cfg(kani) not cfg(test), so they cannot be called from a normal integration test. The common test module referenced by proofs_invariants.rs (line 7: mod common) does not exist in the workspace under the engine/tests path. Attempting to call permissionless_progress_not_atomic to drive the ordering would require constructing a fully valid market+account state with an undercollateralised candidate, which in turn requires internal helper access (set_pnl, position_basis_q mutations) that are similarly gated behind cfg(kani). No public deposit+trade path produces a reliably undercollateralised candidate whose touch-before-liq-check sequence can be observed from outside. The invariant holds by inspection of lines 8941-8602-8608 but is structurally untestable as a deterministic black-box integration test.