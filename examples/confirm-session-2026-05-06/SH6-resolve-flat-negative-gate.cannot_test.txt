// CANNOT_TEST: <reason> The functions named in the finding (`resolve_flat_negative`, `absorb_protocol_loss` via the described call chain from `touch_account_live_local` step 7) do not exist under those names in the actual engine source at /root/audit_runs/percolator-live/target/engine/src/percolator.rs. The grep searches for `resolve_flat_negative`, `touch_account_live_local`, `flat_negative`, `touch_account`, `eff_pos == 0`, and `pnl < 0` all returned zero matches. The engine does contain `use_insurance_buffer` (line 4811) and `absorb_protocol_loss` (line 4845), but the specific gating function `resolve_flat_negative` and the `touch_account_live_local` entry point named in the hypothesis do not exist in the codebase under those identifiers. The hypothesis references line numbers (4770-4785, 4822-4848) from a different revision of the source than what is present in the workspace. Without the named functions existing, it is impossible to write a deterministic Rust test that exercises or verifies the claimed call-chain guard.