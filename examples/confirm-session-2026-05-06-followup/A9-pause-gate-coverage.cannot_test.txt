// CANNOT_TEST: The A9-pause-gate-coverage hypothesis assumes the engine has a pause/halt mechanism (a `paused` boolean field on RiskParams or RiskEngine, a `RiskError::Paused` variant, or a similar gate that mutating entrypoints must check before proceeding). Investigation of the engine source at /root/audit_runs/percolator-live/target/engine/src/percolator.rs found ZERO matches for any of: "pause", "Pause", "PAUSE", "paused", "Paused", "halt", "Halt", "halted", "Halted", "frozen", "Frozen", "stop", "Stopped" across the entire engine tree (percolator.rs, risk_params.rs, account.rs, all 17 source files). The RiskEngine struct (percolator.rs:736-880) has no pause field. The RiskParams struct (risk_params.rs:1-42) has no pause field. The RiskError enum (percolator.rs:886-898) has no Paused variant. No public method checks or sets a pause flag. The Percolator engine deliberately has NO pause mechanism — pausing is a wrapper-layer concern (matters of policy live in aeyakovenko/percolator-prog, not in the engine itself). Therefore there is no engine-level pause-gate to audit coverage for, and the hypothesis is structurally untestable as written. The recon agent's TRUE/HIGH verdict on this hypothesis was a confabulation — it imagined an invariant that the codebase does not have. The confirm agent caught this by direct grep over the source. Reclassifying recon's TRUE as "premise does not match codebase" rather than a real invariant violation.
