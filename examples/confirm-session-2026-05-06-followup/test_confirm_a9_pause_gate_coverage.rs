The finding's premise — that there is a `paused` flag, `config.paused`, `RiskError::Paused`, or any pause/unpause mechanism in the engine — is entirely absent from the codebase. There is no such field anywhere in the repository. A deterministic Rust test for this finding cannot be written against the actual engine.

// CANNOT_TEST: The A9 finding assumes a `paused` boolean field on the engine config and a corresponding `RiskError::Paused` variant. Neither exists anywhere in the codebase: grep for "pause", "Pause", "PAUSE", "halt", "Halt", "frozen", "Frozen" across the entire /root/audit_runs/percolator-live tree returns zero matches. The `RiskEngine` struct (percolator.rs:736-880) has no pause field; the `RiskError` enum (percolator.rs:886-898) has no Paused variant; no public method checks or sets a pause flag. The engine has no pause mechanism to audit gate coverage for.